pub struct Transfer {}

impl Transfer {
    fn new() -> Self {
        return Transfer {};
    }

    fn run(transform: domain::public_service::Transform) -> String {
        // transform.
        return "".to_string();
    }
}